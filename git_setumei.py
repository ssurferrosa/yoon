# プル(updateさせる）、プッシュ(updateする)

# # clone: git clone UIR (紐づけの観点でこれが便利)

#statusの概念(saveの感じ)
    #. 3
    # git add. (現在の（全部）ファイルをstatusされる（commitの対象を追加する感じ))
    # git status で確認できる
#commit (add した後で出来る)
    # git commit - m '話したいメッセージ'
    # git push 
#???? git log ?? ?????? git reset [hash] --hard??


def main ():
    print("hello git_hub!!")